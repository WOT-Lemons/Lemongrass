[project]
name = "lemongrass"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "influxdb-client~=1.47",
    "race-monitor~=0.1",
]

[dependency-groups]
pi = ["obd~=0.7"]
race = ["pandas~=2.3"]
